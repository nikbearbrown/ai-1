# Figure Plan — Chapter 8: The Human Rewrite — The Seam

*CAJAL Image Suggest (silent /scan). Density: 3 figures recommended — Mixed.*

## Figure 8.1 — The Combined Test: fourteen items in four groups (Critical, VG)
**Trigger:** "The Combined Test — fourteen items... Fourteen items in four groups, reproduced as a scorecard in Appendix F." The chapter's own embedded comment requests this as a TABLE scorecard. This is a textbook VG signal: the reader cannot confirm the 14-into-4 grouping (Group A: items 1–4, Group B: 5–8, Group C: 9–11, Group D: 12–14) from prose — the uneven group sizes (4/4/3/3) are exactly the structure that must be shown to be verified. Core to the chapter; this test IS the chapter's instrument.
**Figure type:** hierarchy/taxonomy (scorecard structure)
**SCOPE**
- S: single-column 89mm, 300 DPI, flat vector
- C: exactly 4 group blocks — Group A (4 item rows), Group B (4 item rows), Group C (3 item rows), Group D (3 item rows); total 14 item rows. Each item row carries one trailing Pass/Fail cell. (4 group headers + 14 rows = the labeled components; rows are visually subordinate to the 4 headers, which keeps named components within bound.)
- O: vertical stack of 4 group blocks top-to-bottom; within each block, item rows indented beneath the group header; a single narrow right-hand column reserved for the Pass/Fail mark across all rows
- P: flat vector, Okabe-Ito — group headers Black #000000, item-row rules Sky Blue #56B4E9, the reserved Pass/Fail column tinted Orange #E69F00; uniform 1pt strokes; white background; unannotated (blank cells for later typography)
- E: do NOT include the `[verify]` caveat about the list being non-canonical; do NOT depict the five failure-mode rewrite moves (separate figure), the three-pass example, or the "three signals to stop" — those are distinct concepts; no per-item icons
**Negative prompt:** text labels, words, gibberish letters, titles, captions, decorative borders, drop shadows, gradient backgrounds, 3D perspective, red-green combinations, rainbow scales, hand-drawn styles, watermarks

## Figure 8.2 — Five failure modes mapped to their rewrite moves (Important, VG)
**Trigger:** "Chapter 7 named the five Cowork failure modes diagnostically... This chapter names them prescriptively." Each mode is paired with a specific rewrite move: voice drift → specificity injection; fabricated specificity → cite/replace/cut; missing domain judgment → one judgment per page; padded middle → cut 25%, restructure; bridge questions → name the next chapter's deliverable. The before→after pairing is the prescriptive payload and is spread across five sub-sections.
**Figure type:** comparison panels (two-column failure→move mapping)
**SCOPE**
- S: single-column 89mm, 300 DPI, flat vector
- C: exactly 5 paired rows — each row = a left cell (failure mode) and a right cell (rewrite move), joined by a single arrow (→). The five: voice drift→specificity injection; fabricated specificity→cite/replace/cut; missing domain judgment→one judgment per page; padded middle→cut 25%; bridge question→name next deliverable.
- O: two stacked columns (left = failure, right = move), 5 rows; one → arrow per row pointing left-to-right (failure transformed into move)
- P: flat vector, Okabe-Ito — left "failure" cells Vermillion #D55E00, right "move" cells Bluish Green #009E73, arrows Black #000000; uniform 1pt strokes; white background; unannotated
- E: do NOT include the sixth "sycophancy reversal" move (off the official list, like Ch 7); do NOT depict the Combined Test, the three-pass example, or the diagnostic tests from Ch 7 (this figure is prescriptive only); no example sentence text baked in
**Negative prompt:** text labels, words, gibberish letters, titles, captions, decorative borders, drop shadows, gradient backgrounds, 3D perspective, red-green combinations, rainbow scales, hand-drawn styles, watermarks

## Figure 8.3 — The three-pass rewrite progression (Important, MC)
**Trigger:** "Three passes — one chapter rewritten" and the embedded comment `<!-- → [IMAGE: Side-by-side spread of the three-pass progression — Cowork draft, first-pass, second-pass — with marginal annotations matching each revision to the Combined Test item it addresses] -->`. A 3-state progression (Cowork draft → first-pass structural → second-pass voice) where the deltas between states (fix-failure vs. argument/voice reconstruction) are the learning target. The chapter explicitly maps the two deltas to Sommers's novice/expert revision distinction.
**Figure type:** timeline/progression (three panels, left to right)
**SCOPE**
- S: single-column 89mm, 300 DPI, flat vector (three side-by-side panels; if width-constrained, stack vertically — see Notes)
- C: exactly 3 panels — (1) Cowork draft, (2) first-pass (structure fixed, voice generic), (3) second-pass (voice present, test passed) — connected by 2 transition arrows; each arrow labeled-position reserved for the delta type (delta 1 = "fix failure" / novice; delta 2 = "argument + voice" / expert).
- O: three equal panels left-to-right; → arrow between panel 1→2 and panel 2→3; the two arrows visually differentiated to show the novice-vs-expert delta distinction
- P: flat vector, Okabe-Ito — panel 1 (draft) Black #000000 outline, panel 2 Sky Blue #56B4E9, panel 3 Blue #0072B2; the two transition arrows: arrow 1 Orange #E69F00, arrow 2 Bluish Green #009E73 (to mark the qualitative difference between the deltas); uniform 1pt strokes; white background; unannotated (panels are blank framed blocks; marginal annotation applied later in layout)
- E: do NOT bake in the actual paragraph prose of the three passes; do NOT depict the Combined Test items, the five rewrite moves, or the "rewrite moves are teachable" craft list; exclude the blank-page counter-move
**Negative prompt:** text labels, words, gibberish letters, titles, captions, decorative borders, drop shadows, gradient backgrounds, 3D perspective, red-green combinations, rainbow scales, hand-drawn styles, watermarks

## Video candidates
FIGURE 8.3 — VIDEO CANDIDATE — transition mechanism is the learning target — the pedagogical point is the *delta* between passes (novice word-level fixes vs. expert argument/voice reconstruction), so an animated reveal of what changes from draft→pass1→pass2 teaches the mechanism a static three-panel can only imply. Static remains the publication default (EPUBs do not execute JS); flag only as an optional companion-web asset.

## Notes
- Total: 3 figures (1 from the chapter's embedded IMAGE comment = Fig 8.3; 1 from the embedded TABLE comment = Fig 8.1; 1 newly detected = Fig 8.2).
- Two-concepts-in-one watch: the Combined Test (Fig 8.1) and the five rewrite moves (Fig 8.2) are genuinely distinct instruments and must stay separate — do not collapse into one "everything" figure.
- Split point: Fig 8.1 sits at the design ceiling (4 group headers as primary components, 14 rows subordinate). Do NOT add per-item Pass/Fail evidence text as a third column — that would push it past 6–8 primary components and into a full data table; keep evidence as prose.
- The PQ-flavored stats in the closing (10–20% lexical-diversity reduction, Padmakumar/He) are single isolated figures cited inline, not a comparison set — not figure-worthy; deliberately not proposed.
- Fig 8.3 layout: prefer three horizontal panels; if 89mm single-column forces illegibility, the split point is to stack the three panels vertically with downward arrows rather than dropping a panel.
